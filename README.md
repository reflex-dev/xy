# Customizability audit — verification screenshots

Evidence for the stacked PRs off `alek/fix-csp-font-src`. This branch carries
images only; it is never merged, so the code PRs stay free of binaries.

Each pair is the same chart built by the same script, rendered once against
`origin/main` and once against the fix.
