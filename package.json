{
  "name": "xy-dev-tools",
  "private": true,
  "description": "Development-only dependencies for xy: the vite/typescript client build toolchain and browser test runner",
  "scripts": {
    "build": "node js/build.mjs",
    "typecheck": "tsc -p js/tsconfig.json"
  },
  "devDependencies": {
    "playwright": "1.61.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
