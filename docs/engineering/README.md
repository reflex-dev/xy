# Engineering Documentation

This directory contains XY's implementation dossiers, benchmarks, compatibility
matrices, release-readiness records, and contributor internals. The public
documentation lives directly under `docs/`, while the Reflex application that
renders it lives in `docs/app/`.
